<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_background"
    tools:context=".NotificationsSettingsFragment">

    <!-- TODO: Update blank fragment layout -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/fragment_notifications_settings__notifications_title"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="5dp"
            android:text="@string/notifications_title"
            android:textColor="@color/teal_200" />

        <LinearLayout
            android:id="@+id/fragment_notifications_settings__notifications_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toBottomOf="@id/fragment_notifications_settings__notifications_title" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/fragment_notifications_settings__new_notification_button"
            style="@style/login_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/new_notification_button_text"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/fragment_notifications_settings__notifications_layout" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>